// Legacy Dock component - replaced by MinimalDock
import React from 'react';

const Dock: React.FC = () => {
  // This component is no longer used - MinimalDock has replaced it
  return null;
};

export default Dock;